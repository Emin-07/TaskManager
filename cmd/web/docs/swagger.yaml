info:
  contact: {}
paths:
  /tasks/:id:
    get:
      consumes:
      - application/json
      description: get task from db
      produces:
      - application/json
      responses:
        "200":
          description: 'task": {Id, Title, Text, Priority, Expires}}'
          schema:
            type: string
      summary: get task
      tags:
      - tasks
swagger: "2.0"
